
// import 'package:flutter/widgets.dart';

// abstract final class AppLocalization {
//   static const String path = 'assets/translations';
//   static const Locale en = Locale('en');
//   static const Locale vi = Locale('vi');

//   static Locale? initLocale;
//   static const Locale fallback = en;

//   static const supported = [en, vi];

//   static Future<void> updateLocate(BuildContext context, Locale locale) async {
//     // context.setLocale(locale);
  
//   }

//   static Future<void> toEn(BuildContext context) => updateLocate(context, en);

//   static Future<void> toVi(BuildContext context) => updateLocate(context, vi);

//   static Future<void> toDefault(BuildContext context) => updateLocate(context, context.deviceLocale);

//   static Locale getCurrentLocale(BuildContext context) => context.locale;
// }
